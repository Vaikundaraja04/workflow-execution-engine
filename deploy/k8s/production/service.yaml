apiVersion: v1
kind: Service
metadata:
  name: workflow-engine-api
  namespace: workflow-engine
spec:
  type: ClusterIP
  selector:
    app: workflow-engine-api
  ports:
  - name: http
    port: 80
    targetPort: 3000