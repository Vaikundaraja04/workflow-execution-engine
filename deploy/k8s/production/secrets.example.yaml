# Template only - never commit real values. Populate with:
#   kubectl -n workflow-engine create secret generic workflow-engine-secrets \
#     --from-literal=mongodb-uri='mongodb://...' --from-literal=redis-url='redis://...' \
#     --from-literal=auth-jwt-secret="$(openssl rand -base64 48)" \
#     --from-literal=webhook-secret-key="$(openssl rand -base64 48)"
apiVersion: v1
kind: Secret
metadata:
  name: workflow-engine-secrets
  namespace: workflow-engine
type: Opaque
stringData:
  mongodb-uri: "REPLACE_ME"
  redis-url: "REPLACE_ME"
  auth-jwt-secret: "REPLACE_WITH_32_PLUS_CHAR_RANDOM_VALUE"
  webhook-secret-key: "REPLACE_WITH_32_PLUS_CHAR_RANDOM_VALUE"